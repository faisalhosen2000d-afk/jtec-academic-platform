# Validation

Zod schemas shared between client forms and server-side checks (file upload
constraints, registration, material metadata, results, notices, etc.).
Implemented alongside each domain's phase (I, M, N, ...).
